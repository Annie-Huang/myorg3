import React from 'react';

import './testlib.css';

/* eslint-disable-next-line */
export interface TestlibProps {}

export const Testlib = (props: TestlibProps) => {
  return (
    <div>
      <h1>Welcome to testlib component!</h1>
    </div>
  );
};

export default Testlib;
